#ifndef MAIN_H
#define MAIN_H

/* Preprocessor Directive */
#include <limits.h>
#include <stdio.h>
#include <stdarg.h>
#include <unistd.h>

/* Prototypes */
int _printf(const char *format, ...);

#endif /* MAIN_H */
